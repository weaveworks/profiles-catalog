SHELL: /bin/bash
.ONESHELL:


OS := $(shell uname | tr '[:upper:]' '[:lower:]')
CONFDIR="${PWD}/.conf"
BINDIR="${PWD}/.bin"
REPODIR="${PWD}/charts"
INFRASTRUCTURE?="kind"

test:
	@echo "Testing platform"
	bash .bin/test-platform.sh

sort-layers:
	@echo "Sorting charts by layers"
	bash .bin/sort-layers.sh ${INFRASTRUCTURE}